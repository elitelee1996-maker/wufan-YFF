# 简道云API宝宝书

> **内部 Skill | version: 1.0 | category: productivity**

## 用途

简道云开放平台API完整参考手册。当涉及简道云API调用、字段映射、数据写入、文件上传、流程操作时使用。

## 触发词

- "简道云API"
- "jdy API"
- "数据写入"
- "文件上传"
- "字段格式"

## 输入/输出

根据具体任务由 Agent 按需调用，输入为用户自然语言，输出为对应 Skill 执行结果。

## 安全边界

- 不涉及文件删除、数据修改等高危操作
- 按 SKILL.md 内定义的工作流执行
- 不确定时按 SKILL.md 内指引返回
